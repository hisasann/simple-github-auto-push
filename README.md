# `simple-github-auto-push`

Simple github actions to push to github.

## Usage

```
- uses: hisasann/simple-github-auto-push@main
  env:
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    BRANCH_NAME: #optional defaults to main
```
